{{#if isEditing}}
  <form {{action 'save' on='submit'}}>
    {{yield (action 'save') (action 'cancel')}}
    <button {{action 'save'}}>✓</button>
    <button {{action 'cancel'}}>x</button>
  </form>
{{else}}
  <a href="#" class="status internal--edit-button" {{action 'edit'}}>
    {{ clickableValue }}
  </a>
{{/if}}
